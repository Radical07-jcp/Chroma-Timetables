<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The app is 100% Jetpack Compose now — all real theming (light/dark/black, colors,
         typography, shapes) lives in designsystem/Theme.kt. This XML theme exists only
         because the manifest requires one before the Compose window is up. -->
    <style name="Theme.CromaScheduler" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/ic_launcher_background</item>
    </style>
</resources>
